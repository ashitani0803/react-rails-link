import * as Actions from "./actions"
import initialState from "../store/initialState"

export const LinksReducers = (state = initialState.links, actions) => {
    switch (actions.type) {
        default:
            return state
    }
}
